<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="?attr/colorSurface">

    <!-- 📁 Contenedor del fragmento -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 🎨 Botón para cambiar PALETA (Guinda/Azul) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnTheme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:srcCompat="@drawable/ic_palette"
        android:contentDescription="Cambiar paleta de color"
        android:layout_gravity="bottom|start"
        android:importantForAccessibility="yes"
        android:tint="@android:color/white" />

    <!-- 🌓 Botón para cambiar APARIENCIA (Claro/Oscuro/Sistema)
         👉 Anclado "encima" de btnTheme -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnMode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="82dp"
        app:srcCompat="@drawable/ic_dark_mode"
        android:contentDescription="Cambiar apariencia"
        android:importantForAccessibility="yes"
        android:tint="@android:color/white"
        app:layout_anchor="@id/btnTheme"
        app:layout_anchorGravity="top|center_horizontal" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
